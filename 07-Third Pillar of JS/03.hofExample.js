let arr = [1, 29, 65, 20, 43, 21, 8223, 11, 467, 35, 74, 700, 12];
arr.sort(); // arr.sort() is going to sort the array in lexicographical order (dictonary order)

/**
 *  0 -> A
 *  1 -> B
 *  2 -> C
 *  ...
 */

console.log(arr);
