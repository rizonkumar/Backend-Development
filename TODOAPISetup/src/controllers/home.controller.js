function homePingController(req, res) {
  // collect the request
  // send it to service
  // and then prepare & send response
  return res.json({ msg: "OK" });
}

module.exports = homePingController;
