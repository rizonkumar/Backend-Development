let obj = {
    toString() {
        // by default gives [Object object]
        return "Let's learn JAVASCRIPT"; // still you can override
    },

    valueOf() {
        // by default it returns the same object

    }
}